import 'package:flutter/material.dart';

class AcademiesTabView extends StatelessWidget {
  const AcademiesTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}