import 'package:flutter/material.dart';

class StudiosTabView extends StatelessWidget {
  const StudiosTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [


      ],
    );
  }
}