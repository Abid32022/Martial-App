import 'package:flutter/material.dart';

class AdultsTabView extends StatelessWidget {
  const AdultsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}